import 'package:flutter/material.dart';

class subject_info extends StatelessWidget {
  const subject_info({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
          child: Text("subject details"),
        ),
      ),
    );
  }
}
